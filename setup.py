from setuptools import setup, find_packages

setup(
    name='pyFileIO',
    version='0.0.0',
    license='GPLv3',
    packages=find_packages(),
    url='https://github.com/reilleya/pyFileIO',
    description='A library that provides methods for saving, loading, and migrating application data files.'
)